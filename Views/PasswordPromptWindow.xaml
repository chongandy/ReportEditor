<Window x:Class="ReportEditor.Views.PasswordPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Password required"
        Width="420"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#F7F9FC">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="MessageText"
                   Grid.Row="0"
                   TextWrapping="Wrap"
                   Foreground="#2D3748"
                   Margin="0,0,0,12"/>

        <PasswordBox x:Name="PasswordInput"
                     Grid.Row="1"
                     MinHeight="30"
                     Padding="6,4"
                     VerticalContentAlignment="Center"
                     PasswordChanged="PasswordInput_PasswordChanged"/>

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button x:Name="OkButton"
                    Content="Open"
                    IsDefault="True"
                    MinWidth="80"
                    MinHeight="28"
                    Padding="12,4"
                    Margin="0,0,8,0"
                    IsEnabled="False"
                    Click="Ok_Click"/>
            <Button Content="Cancel"
                    IsCancel="True"
                    MinWidth="80"
                    MinHeight="28"
                    Padding="12,4"
                    Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
